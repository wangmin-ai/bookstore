<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.OrderFormMapper">
    <resultMap id="order_form" type="com.bookstore.pojo.OrderForm">
        <id column="order_id" property="order_id"/>
        <result column="user_id" property="user_id"/>
        <result column="receiver_telephone" property="receiver_telephone"/>
        <result column="receiver_adress" property="receiver_adress"/>
        <result column="receiver_name" property="receiver_name"/>
        <result column="all_cost" property="all_cost"/>
        <result column="all_number" property="all_number"/>
        <result column="order_create_time" property="order_create_time"/>
    </resultMap>

    <!--根据使用者user_id查询订单列表详情-->
    <select id="selectByUserId" resultMap="order_form">
        select * from order_form where user_id = #{user_id}
    </select>

</mapper>